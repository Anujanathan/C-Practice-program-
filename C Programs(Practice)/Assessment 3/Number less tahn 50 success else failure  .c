#include<stdio.h>
int main()
{
    int num,a,b,c,d,first,e,f;
    printf("Enter the number:");
    scanf("%d",&num);
    if(num<50)
    {
        printf("Success");
    }
    else
    {
        printf("Failure");
    }
}
